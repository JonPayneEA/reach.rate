% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flode_classes.R
\name{FlodeSegmentedRating}
\alias{FlodeSegmentedRating}
\title{A fitted segmented rating curve, Hodson et al. (2024) parameterisation (S7)}
\usage{
FlodeSegmentedRating(
  gaugings = (structure(function (.data) 

    stop2(sprintf("S3 class <\%s> doesn't have a constructor.", "data.table"), call =
    NULL), class = "S7_constructor"))(),
  fit_starts = NULL,
  status = "independently_fitted",
  provenance = list(),
  previous = NULL,
  coefficients = (structure(function (.data) 

    stop2(sprintf("S3 class <\%s> doesn't have a constructor.", "data.table"), call =
    NULL), class = "S7_constructor"))(),
  n_segments = integer(0),
  estimate_breakpoints = logical(0)
)
}
\arguments{
\item{gaugings}{Data.table with \code{discharge_cms} and \code{stage_m}
columns -- the gaugings the fit was built from. May optionally
include a \code{gauging_datetime} column (\code{Date} or \code{POSIXct}) recording
when each spot gauging was taken. Nothing in the package currently
reads this column -- it's groundwork for a future age-aware fitting
option (see issue #9). A gauging's own measurement method (current-
meter, ADCP, salt-dilution, index-velocity, ...) is not currently
recorded either -- every gauging is treated identically regardless
of source.}

\item{fit_starts}{Data.table of multi-start fitting attempts, or
\code{NULL} if the fit wasn't produced with \code{multi_start = TRUE}.}

\item{status}{Character. One of \code{"independently_fitted"},
\code{"post_fit_aligned"}, or \code{"constrained_refit"}.}

\item{provenance}{List recording how the fit was made (fitting
method, controls, bounds, tool version).}

\item{previous}{The exact pre-amendment \code{FlodeSegmentedRating} this
was built from, or \code{NULL} (default) for an independently-fitted
result. Nothing in the package currently produces an amended
\code{FlodeSegmentedRating} -- this exists for parity with \link{FlodeRating}.}

\item{coefficients}{Single-row data.table: \code{C}, \code{bp1..bpk},
\code{n1..nk}, and fit diagnostics.}

\item{n_segments}{Integer. Number of segments \code{k}.}

\item{estimate_breakpoints}{Logical. Whether the interior
breakpoints were refit as free parameters (\code{TRUE}) or held fixed
at \code{control} (\code{FALSE}).}
}
\description{
The result of \code{\link[=rate_optimise_segmented]{rate_optimise_segmented()}}: a single joint model
rather than independent limbs, so \verb{@coefficients} is one row (\code{C},
\code{bp1..bpk}, \code{n1..nk}, diagnostics), not one row per segment.
}
