% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate_optimise.R
\name{suggest_breakpoints}
\alias{suggest_breakpoints}
\title{Suggest candidate stage breakpoints for a multi-limb rating fit}
\usage{
suggest_breakpoints(
  discharge_cms,
  stage_m,
  max_breaks = 2L,
  min_obs_per_side = 5L,
  min_improvement = 0.02,
  min_gap = NULL,
  max_candidates = 200L
)
}
\arguments{
\item{discharge_cms, stage_m}{Numeric vectors, as in \code{\link[=rate_optimise]{rate_optimise()}}.}

\item{max_breaks}{Integer. Maximum breakpoints to search for.
Default \code{2L}.}

\item{min_obs_per_side}{Integer. Minimum gaugings required on each
side of a candidate. Default \code{5L}.}

\item{min_improvement}{Numeric. Minimum relative RSS reduction
required. Default \code{0.02}.}

\item{min_gap}{Numeric or \code{NULL}. Minimum stage distance between
selected breakpoints. Default \code{NULL} (5\% of the stage range).}

\item{max_candidates}{Integer. Candidates evaluated per round, capped
and thinned if exceeded. Default \code{200L}.}
}
\value{
A \code{data.table}, one row per candidate evaluated across every
round, with \code{candidate_stage}, \code{score}, \code{improvement},
\code{n_obs_lower}, \code{n_obs_upper}, \code{round}, \code{fit_status}, \code{rank}. The
greedily-selected breakpoints are attached as the
\code{"selected_breaks"} attribute -- use
\code{\link[=suggested_breakpoints_vector]{suggested_breakpoints_vector()}} to extract them.
}
\description{
A heuristic aid for choosing \code{control} in \code{\link[=rate_optimise]{rate_optimise()}}. Reuses
\code{rate_optimise()} directly: for each candidate stage, fits a two-limb
rating there and compares its AIC against the current best model
(penalising the extra parameters, unlike raw RSS). A candidate must
also clear \code{min_improvement}, a minimum relative RSS reduction, to be
eligible. Multiple breakpoints are found iteratively and greedily,
respecting \code{min_gap}. Not a claim that a statistically-supported
breakpoint corresponds to a real physical control change -- that
judgement remains yours.
}
\seealso{
\code{\link[=rate_optimise]{rate_optimise()}}, \code{\link[=suggested_breakpoints_vector]{suggested_breakpoints_vector()}}
}
