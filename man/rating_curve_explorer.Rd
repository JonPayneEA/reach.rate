% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rating_curve_explorer_app.R
\name{rating_curve_explorer}
\alias{rating_curve_explorer}
\title{Launch the interactive rating curve explorer}
\usage{
rating_curve_explorer()
}
\value{
A \code{shiny.appobj}, invisibly. Auto-launches when printed (which
happens automatically if you just call \code{rating_curve_explorer()} at
the console); pass it to \code{\link[shiny:runApp]{shiny::runApp()}} explicitly otherwise.
}
\description{
A teaching/exploration Shiny app: adjust \code{C} (scale), \code{a} (offset),
and \code{n} (exponent) per limb with sliders and watch the curve shape
change live. Add a second or third limb to see whether
independently-fitted segments meet cleanly at a junction, or don't --
the "Align" button rescales the upper limb's \code{C} to close the gap,
mirroring \code{\link[=align_limb_equations]{align_limb_equations()}}. This app does not touch gauging
data or call \code{\link[=rate_optimise]{rate_optimise()}}; it is purely for building intuition
about the equation and the junction-gap problem.

Requires the \pkg{shiny} package (Suggests, not a hard dependency of
this package).
}
\examples{
\dontrun{
rating_curve_explorer()
}

}
