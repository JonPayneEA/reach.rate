% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate_optimise.R
\name{flag_influential_gaugings}
\alias{flag_influential_gaugings}
\title{Flag gaugings that disproportionately steer their limb's fit}
\usage{
flag_influential_gaugings(fit, cooks_mult = 4, leverage_mult = 2)
}
\arguments{
\item{fit}{A \link{FlodeRating} instance.}

\item{cooks_mult, leverage_mult}{Single positive numbers. A gauging is
flagged \code{influential} if its Cook's distance exceeds
\code{cooks_mult / n_obs} (limb-specific \code{n_obs}) or its leverage exceeds
\code{leverage_mult * 3 / n_obs} -- the standard rule-of-thumb cutoffs
(Cook 1977; Belsley, Kuh & Welsch 1980), computed per limb since
\code{n_obs} varies by limb. Defaults \code{4} and \code{2} respectively.}
}
\value{
A new \link{FlodeRating} instance with \verb{@gaugings} columns added:
\code{fitted_cms}, \code{residual_cms}, \code{leverage}, \code{cooks_distance},
\code{influential}.
}
\description{
\code{\link[=plot_rating_residuals]{plot_rating_residuals()}} shows how far off each gauging ends up
\emph{after} fitting; this asks a different question -- how much each
gauging \emph{shaped} the fit to begin with. A gauging can sit right on
its own limb's curve (a small residual) while still having pulled
\code{C}/\code{a}/\code{n} a long way to get there, if nothing else nearby held the
curve in place -- typically a single high or low gauging isolated at
the sparse end of a limb.

Leverage and Cook's distance are standard linear-regression
diagnostics (Cook 1977), adapted here to \code{rate_optimise()}'s
nonlinear fit by the usual local-linearisation trick (St. Laurent &
Cook 1992): the model's Jacobian with respect to \code{C}/\code{a}/\code{n},
evaluated at each gauging, stands in for a design matrix. Leverage
measures how extreme a gauging's position is, independent of fit
quality; Cook's distance combines that with its residual to estimate
how much \code{C}/\code{a}/\code{n} would shift if that one gauging were dropped and
the limb refit -- without actually doing the refit.

A limb with \code{n_obs <= 3} (fewer gaugings than free parameters) has no
defined leverage -- both columns are \code{NA} for its gaugings.
}
\examples{
set.seed(1)
stage_m <- seq(0.3, 3, by = 0.1)
discharge_cms <- 5 * stage_m^1.5 * exp(rnorm(length(stage_m), sd = 0.03))
# one isolated, high gauging at the sparse top end
stage_m <- c(stage_m, 3.5)
discharge_cms <- c(discharge_cms, 5 * 3.5^1.5 * 1.25)

fit <- rate_optimise(discharge_cms, stage_m, n_bounds = c(1.3, 1.7))
flagged <- flag_influential_gaugings(fit)
flagged@gaugings[order(-cooks_distance)][1:3]

}
\references{
Cook, R. D. (1977). Detection of influential observations
in linear regression. \emph{Technometrics}, 19(1), 15-18. St. Laurent,
R. T., & Cook, R. D. (1992). Leverage and superleverage in
nonlinear regression. \emph{Journal of the American Statistical
Association}, 87(420), 985-990. Belsley, D. A., Kuh, E., & Welsch,
R. E. (1980). \emph{Regression Diagnostics}. Wiley.
}
\seealso{
\code{\link[=plot_rating_residuals]{plot_rating_residuals()}} for the complementary
after-the-fact view; \code{\link[=plot_rating_leverage]{plot_rating_leverage()}} to see this plotted;
\code{vignette("leverage_influence_guide")} for a plain-English
explanation with a worked example.
}
