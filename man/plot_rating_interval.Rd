% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rating_curve_demo.R
\name{plot_rating_interval}
\alias{plot_rating_interval}
\title{Plot a fitted rating curve with a bootstrap prediction interval}
\usage{
plot_rating_interval(fit, conf_level = 0.95, n_points = 150L)
}
\arguments{
\item{fit}{A \link{FlodeRating} instance from \code{rate_optimise(..., n_boot > 0)}.}

\item{conf_level}{Numeric in (0, 1). Width of the shaded interval.
Default \code{0.95}.}

\item{n_points}{Integer. Stage points per limb used to draw the band.
Default \code{150L}.}
}
\value{
A \code{ggplot} object, invisibly. Printed as a side effect.
}
\description{
Like \code{rating_plot()}, but for a fit produced with
\code{rate_optimise(..., n_boot = )}: draws the mean discharge curve with a
shaded prediction-interval band (from \code{apply_rating_interval()})
instead of a single point-estimate line, and overlays the gauged
points. This is the plot Hodson et al. (2024)'s Figure 1 shows for
their Bayesian fits; the band here is the bootstrap approximation to
the same idea, not a Bayesian posterior.
}
\examples{
set.seed(1)
stage_m <- seq(0.5, 3.5, by = 0.1)
discharge_cms <- 5 * stage_m^1.6 + rnorm(length(stage_m), sd = 0.05)
fit <- rate_optimise(discharge_cms, stage_m, n_boot = 200L, boot_seed = 1L)
plot_rating_interval(fit)

}
\seealso{
\code{\link[=rate_optimise]{rate_optimise()}}, \code{\link[=bootstrap_to_table]{bootstrap_to_table()}},
\code{apply_rating_interval()} (in the \code{apply_rating} module)
}
