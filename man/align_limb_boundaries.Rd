% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gap_check.R
\name{align_limb_boundaries}
\alias{align_limb_boundaries}
\title{Relocate a junction to where two limb equations actually cross}
\usage{
align_limb_boundaries(rating_dt, junctions = NULL)
}
\arguments{
\item{rating_dt}{A \link{FlodeRating}, a \link{FlodeRatingTable}, or a plain
data.frame/data.table with columns \code{lower_level}, \code{upper_level},
\code{C}, \code{a}, \code{n} (the same shape expected by \code{\link[=expand_rating_table]{expand_rating_table()}}
and \code{\link[=align_limb_equations]{align_limb_equations()}}). Limbs must be contiguous:
\code{upper_level[i]} must equal \code{lower_level[i + 1]}.}

\item{junctions}{Integer vector, or \code{NULL} (default). Which junctions to
attempt, numbered the same way as \code{\link[=detect_rc_gaps]{detect_rc_gaps()}}'s \code{junction}
column (\code{1} = between limbs 1 and 2, \code{2} = between limbs 2 and 3, and
so on). \code{NULL} attempts every junction, as before. Any other junction
is left completely unchanged -- useful for leaving a junction alone
deliberately (a known-good join, or one better handled by
\code{\link[=align_limb_equations]{align_limb_equations()}} instead) even where a crossing exists.}
}
\value{
If \code{rating_dt} was a \link{FlodeRating}, a new \link{FlodeRating} with
\verb{@status} \code{"post_fit_aligned"}, \verb{@previous} referencing the exact
original fit, \verb{@gaugings} with \code{limb} \strong{reclassified} against the
relocated boundaries (a moved junction can shift which limb a
gauging belongs to, on either side of it), and \verb{@limbs} diagnostics
recomputed from those reclassified gaugings -- never left
describing a limb's old gauging composition. \verb{@bootstrap}/
\verb{@fit_starts} are dropped (with a warning if either was present),
since they describe a fit that no longer applies. Otherwise (a plain
table or an already-constructed \link{FlodeRatingTable}), a
\link{FlodeRatingTable} with \verb{@status} \code{"post_fit_aligned"} and
\verb{@previous} referencing the exact pre-alignment object this was
built from (\code{NULL} if \code{rating_dt} was a plain table with no prior
\code{FlodeRatingTable} identity).

Either way, \verb{@limbs}/\verb{@table} gains columns:
\code{lower_level_original}/\code{upper_level_original} (renamed
\code{lower_stage_m_original}/\code{upper_stage_m_original} on a \code{FlodeRating}
result, to match its own naming) -- the input boundaries, unchanged
-- and \code{boundary_adjusted} (logical; \code{TRUE} for a limb whose
boundary actually moved). \code{C}, \code{a}, and \code{n} are always unchanged --
only the boundaries are ever moved. Neither the input nor the output
is ever mutated in place.
}
\description{
\code{align_limb_equations()} and \code{resolve_rc_gaps()} both close a junction
gap by changing a \emph{value} (a rescaled \code{C}, or a patched discharge)
at a \emph{fixed} junction stage. \code{align_limb_boundaries()} does the
opposite: it leaves every limb's \code{C}/\code{a}/\code{n} completely untouched, and
instead moves the junction stage itself to wherever the two limbs'
curves genuinely cross -- the stage \code{H} solving \code{C_lower*(H-a_lower)^ n_lower = C_upper*(H-a_upper)^n_upper}. At that stage the two unchanged
curves already agree, so nothing about either equation needs to change.

Unlike \code{align_limb_equations()}'s rescale (which needs a fixed anchor
limb and propagates outward, since rescaling one limb changes the
reference point for the next), boundary relocation has no such
dependency: because no equation is ever changed, junction \code{i}'s
crossing depends only on limbs \code{i} and \code{i + 1}, regardless of what
happens at any other junction. Every junction is resolved
independently -- there is no \code{anchor_limb} argument. By default every
junction is attempted; pass \code{junctions} to restrict this to specific
ones, leaving every other junction's boundary untouched (and silent --
no "no crossing" warning is possible for a junction that was never
attempted).

The search for a crossing is bounded to the union of the two limbs'
own existing stage ranges (never extrapolated further than either limb
was actually fitted over), and above both limbs' zero-flow stage so
\code{(H - a)^n} stays real-valued. When no crossing exists in that range --
the curves may not cross at all, or only outside where either was
fitted -- that junction is left unchanged and a warning is issued
rather than the whole call failing.
}
\examples{
# A small, realistic gap at the breakpoint (limb 2 reads ~5\% high there)
rating_dt <- data.table::data.table(
  lower_level = c(0.0, 1.2),
  upper_level = c(1.2, 4.0),
  C = c(2.5, 2.554),
  a = c(0.0, 0.0),
  n = c(1.50, 1.65)
)
relocated_result <- align_limb_boundaries(rating_dt)
relocated_result@table[, .(lower_level, upper_level, boundary_adjusted)]

# A three-limb table, restricting relocation to junction 2 only (between
# limbs 2 and 3) -- junction 1 is left untouched even though a crossing
# exists there too
rating_dt3 <- data.table::data.table(
  lower_level = c(0.0, 1.2, 2.5),
  upper_level = c(1.2, 2.5, 4.0),
  C = c(2.5, 2.554, 2.337325),
  a = c(0.0, 0.0, 0.0),
  n = c(1.50, 1.65, 1.80)
)
partial_result <- align_limb_boundaries(rating_dt3, junctions = 2L)
partial_result@table[, .(lower_level, upper_level, boundary_adjusted)]

}
\seealso{
\code{\link[=align_limb_equations]{align_limb_equations()}} for closing a junction by rescaling
\code{C} at a fixed stage instead of moving the stage; \code{\link[=resolve_rc_gaps]{resolve_rc_gaps()}}
for the discretised-table-only equivalent; \code{\link[=expand_rating_table]{expand_rating_table()}}
}
