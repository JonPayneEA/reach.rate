% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weir_equations.R
\name{flume_discharge_parshall}
\alias{flume_discharge_parshall}
\title{Discharge through a standard Parshall flume (free flow)}
\usage{
flume_discharge_parshall(
  head_m,
  throat_width_m,
  u_cd,
  u_head_m,
  coverage_k = 2,
  hb_head_m = NULL
)
}
\arguments{
\item{head_m}{Numeric vector. Head over the weir crest, m. Must be positive.}

\item{throat_width_m}{Single positive number. Flume throat width, m.}

\item{u_cd}{Single non-negative number. Relative standard uncertainty
in the discharge coefficient (e.g. \code{0.02} for 2\%). See Description.}

\item{u_head_m}{Single non-negative number. Absolute standard
uncertainty in the measured head, m.}

\item{coverage_k}{Single positive number. GUM coverage factor for the
expanded uncertainty \code{U = coverage_k * u_c}. Default \code{2}
(approximately 95\% for a normally-distributed measurand, the
conventional GUM default -- JCGM 100:2008 §2.3.6).}

\item{hb_head_m}{Numeric vector matching \code{head_m} in length, or \code{NULL}
(default). Downstream head, for the free-flow submergence check
described above. \code{NULL} skips the check.}
}
\value{
As \code{\link[=weir_discharge_rectangular]{weir_discharge_rectangular()}}.
}
\description{
The general free-flow discharge equation for a standard Parshall
flume with throat width between 1 and 8 feet (0.3048-2.4384 m):

\deqn{Q = 2.340 \, W^{1.026} \, H_a^{1.522}}

(SI form; \eqn{W} is throat width, \eqn{H_a} is the upstream head
gauged at the standard measuring station. Derived here from the
equation's published US customary form,
\eqn{Q_{cfs} = 4 \, W_{ft}^{1.026} H_{a,ft}^{1.522}}, by direct unit
conversion.) \code{throat_width_m} outside 0.3048-2.4384 m warns, since
this general approximation is only confirmed within that range --
smaller and larger standard sizes have their own individually
tabulated \code{C}/\code{n} coefficients (USBR \emph{Water Measurement Manual}, ch.
8, §10; ISO 9826) not reproduced here.

A Parshall flume only measures discharge correctly in free (unsubmerged)
flow. If \code{hb_head_m} (the downstream/submerged head) is supplied, this
checks the standard submergence limit for this throat-width range
(\code{hb_head_m / head_m <= 0.7}) and errors rather than applying an
uncorrected free-flow equation to submerged conditions -- submerged-flow
correction is not implemented.

Uncertainty follows the same GUM quadrature as the other structures
(head exponent \code{1.522}). A 2021 Monte Carlo study (Ferreira et al.)
found roughly \eqn{\pm 2\%} for well-built laboratory flumes, rising to
\eqn{\pm 3\text{-}5\%} for realistic field installations -- context for
\code{u_cd}'s scale, not a default; both \code{u_cd} and \code{u_head_m} are required.
}
\examples{
flume_discharge_parshall(
  head_m = c(0.15, 0.3, 0.45), throat_width_m = 0.3048,
  u_cd = 0.03, u_head_m = 0.005
)

}
\references{
USBR \emph{Water Measurement Manual}, ch. 8, §10. Ferreira,
H.M. et al. (2021), Monte Carlo uncertainty analysis of Parshall
flume discharge. JCGM 100:2008 (GUM).
}
\seealso{
\code{\link[=weir_discharge_rectangular]{weir_discharge_rectangular()}}, \code{\link[=weir_discharge_vnotch]{weir_discharge_vnotch()}},
\code{\link[=weir_discharge_cipoletti]{weir_discharge_cipoletti()}}; \code{vignette("weir_flume_guide")}.
}
