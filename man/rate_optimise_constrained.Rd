% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate_optimise.R
\name{rate_optimise_constrained}
\alias{rate_optimise_constrained}
\title{Fit a multi-limb rating with junction continuity built into the fit}
\usage{
rate_optimise_constrained(
  discharge_cms,
  stage_m,
  control = NULL,
  anchor_limb = 1L,
  ...
)
}
\arguments{
\item{discharge_cms, stage_m, control}{As in \code{\link[=rate_optimise]{rate_optimise()}}.}

\item{anchor_limb}{Integer. Row index of the limb left unconstrained.
Default \code{1L}.}

\item{...}{Passed to \code{rate_optimise()}'s initial (unconstrained) fit.
\code{n_boot} is accepted but the resulting bootstrap draws describe the
\emph{unconstrained} fit and are dropped with a warning rather than
presented alongside updated point estimates; \code{fit_starts} is
dropped silently for the same reason. \code{n_starts_attempted}/
\code{n_starts_converged}/\code{selected_start_id} are set to \code{NA} and
\code{near_bound} is recomputed for any limb this function actually
refits, since the constrained refit is always single-start.}
}
\value{
A \link{FlodeRating} instance in the same shape as
\code{rate_optimise()}'s, plus an \code{aligned} logical column in \verb{@limbs}
(\code{FALSE} for \code{anchor_limb}, \code{TRUE} for every refit limb) and
\verb{@status} \code{"constrained_refit"}. If a constrained refit fails to
converge for some limb, that limb's original unconstrained fit is
kept and a warning is issued.
}
\description{
\code{rate_optimise()} fits each limb independently, which is exactly why
junction gaps exist to detect (\code{detect_rc_gaps()}) and two ways exist
to close them (\code{resolve_rc_gaps()}'s table patch,
\code{align_limb_equations()}'s \code{C}-only rescale). Neither of those two
produces an \emph{optimised} rating once gaps are closed. This does the
constrained version properly: starting from \code{rate_optimise()}'s
independent fit, every limb except \code{anchor_limb} is \emph{refit} against
its own gaugings, subject to matching its already-finalised
neighbour's discharge exactly at their shared junction stage --
propagating outward from the anchor exactly as \code{align_limb_equations()}
does. The constraint is enforced by reparameterising the model:
\code{Q = Q_target * ((H + a) / (brk + a))^n}, which equals \code{Q_target} at
\code{H = brk} for any \code{a}/\code{n}, so \code{nlsLM} is free to optimise both over
the limb's own gaugings while continuity holds automatically.
}
\seealso{
\code{\link[=rate_optimise]{rate_optimise()}}, \code{align_limb_equations()} (in the
\code{gap_check} module) for the closed-form, no-refit alternative
}
