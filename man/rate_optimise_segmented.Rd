% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate_optimise_segmented.R
\name{rate_optimise_segmented}
\alias{rate_optimise_segmented}
\title{Fit a rating curve with Hodson et al. (2024)'s segmented parameterisation}
\usage{
rate_optimise_segmented(
  discharge_cms,
  stage_m,
  gauging_datetime = NULL,
  control = NULL,
  estimate_breakpoints = FALSE,
  multi_start = TRUE,
  objective = c("absolute", "relative"),
  ...
)
}
\arguments{
\item{discharge_cms, stage_m}{Numeric vectors, as in \code{\link[=rate_optimise]{rate_optimise()}}.}

\item{gauging_datetime}{\code{Date}/\code{POSIXct} vector or \code{NULL}, as in
\code{\link[=rate_optimise]{rate_optimise()}}. Default \code{NULL}.}

\item{control}{Numeric vector or \code{NULL}. Interior segment breakpoints.}

\item{estimate_breakpoints}{Logical. If \code{TRUE}, interior breakpoints
are refit as free parameters. Default \code{FALSE}.}

\item{multi_start}{Logical. If \code{TRUE} (default), fits from several
starting-value profiles and keeps the best converged, domain-valid
result. This matters more here than for \code{rate_optimise()}'s
independent-limb fits: this model's loss surface is genuinely
non-convex.}

\item{objective}{Character. \code{"absolute"} (default) minimises absolute
discharge error, as before. \code{"relative"} fits \code{log(discharge_cms)}
residuals instead -- see \code{\link[=rate_optimise]{rate_optimise()}} for the rationale.
Requires \code{discharge_cms} to be strictly positive. Reported
diagnostics (\code{rmse_cms}, \code{r_squared}) are always recomputed on the
natural discharge scale regardless of \code{objective}. Not a default --
existing callers see no change in behaviour.}

\item{...}{Passed to \code{minpack.lm::nls.lm.control()}.}
}
\value{
A \link{FlodeSegmentedRating} instance. \verb{@coefficients} is a
one-row data.table: \code{C}, \code{bp1..bpk}, \code{n1..nk}, \code{rmse_cms},
\code{r_squared}, \code{n_obs}, \code{n_starts_attempted}, \code{n_starts_converged},
\code{selected_start_id}. \verb{@gaugings} holds the input data (and
\code{gauging_datetime} if supplied). \verb{@fit_starts}
holds every multi-start attempt when \code{multi_start = TRUE}.
}
\description{
The rest of this toolkit (\code{rate_optimise()}) fits each limb of a
rating independently over disjoint stage ranges. This is a
structurally different model, adapted from the log-additive segmented
power law in Hodson et al. (2024): every segment contributes
multiplicatively across the whole fitted range rather than owning a
disjoint stage band --

\deqn{Q = C \cdot \max(H - bp_1, 0)^{n_1} \cdot
  \prod_{j \ge 2} (\max(H - bp_j, 0) + 1)^{n_j}}

\code{bp_1} is the stage of zero flow. Each later factor equals exactly 1
(no effect) at and below its own breakpoint \code{bp_j}. Continuity is
therefore automatic, not imposed afterward.

\strong{What this does not reproduce}: Hodson et al. fit this model by
full Bayesian inference. This function fits by ordinary nonlinear
least squares (\code{nlsLM}) instead -- no priors, no posterior. \code{C},
\code{bp_1}, and every exponent are genuine free parameters. The interior
breakpoints \code{bp_2..bp_k} are held fixed at \code{control} by default; set
\code{estimate_breakpoints = TRUE} to free them too, at the cost of the
non-convexity their paper warns about.
}
\examples{
set.seed(1)
stage_m <- seq(0.3, 3.5, by = 0.03)
true_bp1 <- 0.1
q1 <- 4 * pmax(stage_m - true_bp1, 0)^1.55
q2 <- (pmax(stage_m - 1.6, 0) + 1)^0.9
q3 <- (pmax(stage_m - 2.4, 0) + 1)^1.1
discharge_cms <- q1 * q2 * q3 * exp(rnorm(length(stage_m), sd = 0.03))
fit_seg <- rate_optimise_segmented(discharge_cms, stage_m, control = c(1.6, 2.4))
fit_seg@coefficients

}
\seealso{
\code{\link[=rate_optimise]{rate_optimise()}} for this toolkit's usual independent-limb
fit; \code{rating_plot()} and \code{apply_rating()} (both in \code{flode_classes})
for the generics this class's methods are registered against
}
