% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flode_classes.R
\name{FlodeRating}
\alias{FlodeRating}
\title{A fitted multi-limb rating curve (S7)}
\usage{
FlodeRating(
  gaugings = (structure(function (.data) 

    stop2(sprintf("S3 class <\%s> doesn't have a constructor.", "data.table"), call =
    NULL), class = "S7_constructor"))(),
  fit_starts = NULL,
  status = "independently_fitted",
  provenance = list(),
  limbs = (structure(function (.data) 

    stop2(sprintf("S3 class <\%s> doesn't have a constructor.", "data.table"), call =
    NULL), class = "S7_constructor"))(),
  bootstrap = NULL
)
}
\arguments{
\item{gaugings}{Data.table with \code{discharge_cms} and \code{stage_m}
columns -- the gaugings the fit was built from.}

\item{fit_starts}{Data.table of multi-start fitting attempts, or
\code{NULL} if the fit wasn't produced with \code{multi_start = TRUE}.}

\item{status}{Character. One of \code{"independently_fitted"},
\code{"post_fit_aligned"}, or \code{"constrained_refit"}.}

\item{provenance}{List recording how the fit was made (fitting
method, controls, bounds, tool version).}

\item{limbs}{Data.table, one row per limb: bounds, coefficients
\code{C}/\code{a}/\code{n}, and fit diagnostics.}

\item{bootstrap}{Data.table of per-draw bootstrap coefficients, or
\code{NULL} if the fit wasn't produced with \code{n_boot > 0}.}
}
\description{
The result of \code{\link[=rate_optimise]{rate_optimise()}} (or \code{\link[=rate_optimise_constrained]{rate_optimise_constrained()}}):
one row per limb in \verb{@limbs} (bounds, coefficients \code{C}/\code{a}/\code{n}, fit
diagnostics, and -- when produced by \code{rate_optimise_constrained()} --
alignment provenance), the raw gaugings in \verb{@gaugings}, and
bootstrap draws in \verb{@bootstrap} when \code{rate_optimise(..., n_boot = )}
was used.
}
