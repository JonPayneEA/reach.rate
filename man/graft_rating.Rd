% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graft_rating.R
\name{graft_rating}
\alias{graft_rating}
\title{Graft a freshly-fitted rating onto a pre-existing one above its gauged range}
\usage{
graft_rating(
  new_rating,
  existing_rating,
  search_range = NULL,
  on_no_crossing = c("error", "join_at_top")
)
}
\arguments{
\item{new_rating}{A \link{FlodeRating} or \link{FlodeRatingTable} -- the freshly
fitted rating, trusted only up to its own top boundary.}

\item{existing_rating}{A \link{FlodeRatingTable}, or a plain
data.frame/data.table with columns \code{lower_level}, \code{upper_level},
\code{C}, \code{a}, \code{n} (same shape as \code{\link[=expand_rating_table]{expand_rating_table()}}'s input). Only
limbs whose \code{upper_level} exceeds \code{new_rating}'s top boundary are
used; it is an error if none qualify.}

\item{search_range}{\code{NULL} (default), or a numeric \code{c(lower, upper)}
bracket to search for the crossing stage, overriding the default
\verb{c(new_rating's top boundary, first retained existing limb's upper_level)}.}

\item{on_no_crossing}{Character. What to do when no crossing exists
within \code{search_range}. \code{"error"} (default) stops. \code{"join_at_top"}
joins exactly at \code{new_rating}'s top boundary instead, accepting
whatever discontinuity results there, with a warning naming the
resulting jump in discharge.}
}
\value{
A \link{FlodeRatingTable} with \verb{@status} \code{"grafted"}, \verb{@previous}
referencing \code{new_rating} exactly as passed in, and \verb{@table} gaining
a \code{source} column (\code{"new"}/\code{"existing"}) marking which side each
limb came from.
}
\description{
Real stations are sometimes only re-gauged, and re-fitted, up to some
stage -- extreme floods above that stay described by whatever rating
was already in force there. \code{graft_rating()} joins the two: it finds
the stage where \code{new_rating}'s top limb and \code{existing_rating}'s lowest
still-relevant limb actually cross (the same crossing search
\code{\link[=align_limb_boundaries]{align_limb_boundaries()}} uses for a junction inside one table, here
used to join two different ones), trims/extends both limbs to that
stage, and returns one contiguous table.

This is deliberately different from \code{\link[=align_limb_equations]{align_limb_equations()}}/
\code{\link[=align_limb_boundaries]{align_limb_boundaries()}}, which reconcile a junction \emph{within} one
already-built table. \code{graft_rating()} combines two independently
sourced ratings -- one just fitted, one pre-existing -- into a new one.

The result is always a \link{FlodeRatingTable}, even when \code{new_rating} was a
\link{FlodeRating}: the grafted-on limbs come from \code{existing_rating}, which
carries no gaugings of its own, so a \code{FlodeRating} result would be
honest about some limbs and silently false about others. \code{new_rating}
itself, ungrafted, remains the right object for diagnostics on the
newly-fitted part.
}
\examples{
new_table <- data.table::data.table(
  lower_level = c(0.0, 1.5), upper_level = c(1.5, 3.41),
  C = c(2.5, 2.831), a = c(0.0, -0.886), n = c(1.50, 1.935)
)
existing_table <- data.table::data.table(
  lower_level = 3.41, upper_level = 6.0,
  C = 0.3128, a = 0, n = 4.095
)
grafted <- graft_rating(new_table, existing_table)
grafted@table[, .(lower_level, upper_level, C, a, n, source)]

}
\seealso{
\code{\link[=align_limb_boundaries]{align_limb_boundaries()}}, whose crossing search this
function reuses, for reconciling a junction within one table rather
than joining two different ones.
}
