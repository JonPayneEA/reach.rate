% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate_optimise.R
\name{plot_rating_leverage}
\alias{plot_rating_leverage}
\title{Plot each gauging's leverage against its influence on the fit}
\usage{
plot_rating_leverage(fit, cooks_mult = 4, leverage_mult = 2)
}
\arguments{
\item{fit}{A \link{FlodeRating} instance.}

\item{cooks_mult, leverage_mult}{Single positive numbers. A gauging is
flagged \code{influential} if its Cook's distance exceeds
\code{cooks_mult / n_obs} (limb-specific \code{n_obs}) or its leverage exceeds
\code{leverage_mult * 3 / n_obs} -- the standard rule-of-thumb cutoffs
(Cook 1977; Belsley, Kuh & Welsch 1980), computed per limb since
\code{n_obs} varies by limb. Defaults \code{4} and \code{2} respectively.}
}
\value{
A \code{ggplot} object, invisibly.
}
\description{
A scatter of Cook's distance against leverage, one facet per limb,
point size scaled by Cook's distance and colour marking gaugings past
\code{\link[=flag_influential_gaugings]{flag_influential_gaugings()}}'s default cutoffs -- the two quantities
behind that function's \code{influential} flag, plotted directly rather
than collapsed to one flag. See \code{vignette("leverage_influence_guide")}
for how to read this plot without the statistics background the
terms usually assume.
}
\seealso{
\code{\link[=flag_influential_gaugings]{flag_influential_gaugings()}}, \code{\link[=plot_rating_residuals]{plot_rating_residuals()}}.
}
