% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate_optimise.R
\name{flag_extrapolated_limbs}
\alias{flag_extrapolated_limbs}
\title{Flag rating limbs that extrapolate substantially beyond their gaugings}
\usage{
flag_extrapolated_limbs(
  fit,
  tol_frac = 0.05,
  operational_lower_m = NULL,
  operational_upper_m = NULL
)
}
\arguments{
\item{fit}{A \link{FlodeRating} instance.}

\item{tol_frac}{Numeric. Fraction of a limb's stage width the
unsupported gap may reach before being flagged. Default \code{0.05}.}

\item{operational_lower_m, operational_upper_m}{Numeric or \code{NULL}.}
}
\value{
A new \link{FlodeRating} instance with \verb{@limbs} columns added:
\code{gauged_min_stage_m}/\code{gauged_max_stage_m}, \code{lower_unsupported_m}/
\code{upper_unsupported_m}, \code{lower_unsupported_frac}/
\code{upper_unsupported_frac}, \code{extrapolates_below_range}/
\code{extrapolates_above_range} (identical on every row -- a whole-fit
property, not per-limb), and \code{doubtful}.
}
\description{
Assesses support within each limb's own declared bounds (unsupported
distance at an internal breakpoint) and, separately, whether the
whole fitted rating's combined span fails to reach an operational
range if supplied, else the complete gauged range.
}
