% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weir_equations.R
\name{weir_discharge_vnotch}
\alias{weir_discharge_vnotch}
\title{Discharge over a V-notch (triangular) sharp-crested weir}
\usage{
weir_discharge_vnotch(head_m, notch_angle_deg, u_cd, u_head_m, coverage_k = 2)
}
\arguments{
\item{head_m}{Numeric vector. Head over the weir crest, m. Must be positive.}

\item{notch_angle_deg}{Single number, 25-100. Full notch angle, degrees.}

\item{u_cd}{Single non-negative number. Relative standard uncertainty
in the discharge coefficient (e.g. \code{0.02} for 2\%). See Description.}

\item{u_head_m}{Single non-negative number. Absolute standard
uncertainty in the measured head, m.}

\item{coverage_k}{Single positive number. GUM coverage factor for the
expanded uncertainty \code{U = coverage_k * u_c}. Default \code{2}
(approximately 95\% for a normally-distributed measurand, the
conventional GUM default -- JCGM 100:2008 §2.3.6).}
}
\value{
As \code{\link[=weir_discharge_rectangular]{weir_discharge_rectangular()}}.
}
\description{
The Kindsvater-Shen equation, the form ISO, ASTM, and USBR each adopt
for a sharp-crested triangular-notch weir:

\deqn{Q = \frac{8}{15} \sqrt{2g} \, C_e \tan(\theta/2) \, H_e^{5/2},
      \qquad H_e = H + k_h}

where \eqn{\theta} is the full notch angle and \eqn{H_e} is head over
the notch vertex corrected by a small empirical head offset
\eqn{k_h}. Both \eqn{C_e} (dimensionless) and \eqn{k_h} (originally
published in feet; converted to metres internally, with the polynomial
itself evaluated exactly as published) are fitted polynomials in
\eqn{\theta} (degrees), valid for notch angles from 25 to 100 degrees
-- outside that range this errors rather than extrapolating an
unvalidated fit.

Uncertainty follows the same GUM quadrature as
\code{\link[=weir_discharge_rectangular]{weir_discharge_rectangular()}}, with head exponent \code{5/2} (applied to
the corrected head \eqn{H_e}). A relative \eqn{u^*(C_e)} of roughly
1-2\\% is a common literature reference point for a well-installed
V-notch weir -- context, not a default; \code{u_cd} is still required.
}
\examples{
weir_discharge_vnotch(
  head_m = c(0.05, 0.1, 0.15), notch_angle_deg = 90,
  u_cd = 0.02, u_head_m = 0.002
)

}
\references{
Kindsvater, C.E. & Carter, R.W. (1957); Shen, J. (1981),
\emph{Discharge Characteristics of Triangular-Notch Thin-Plate Weirs},
USGS Water-Supply Paper 1617-B. USBR \emph{Water Measurement Manual},
ch. 7, §7. JCGM 100:2008 (GUM).
}
\seealso{
\code{\link[=weir_discharge_rectangular]{weir_discharge_rectangular()}}, \code{\link[=weir_discharge_cipoletti]{weir_discharge_cipoletti()}},
\code{\link[=flume_discharge_parshall]{flume_discharge_parshall()}}; \code{vignette("weir_flume_guide")}.
}
