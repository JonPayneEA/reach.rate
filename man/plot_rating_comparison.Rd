% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_ratings.R
\name{plot_rating_comparison}
\alias{plot_rating_comparison}
\title{Plot a rating comparison: curves and their discharge difference}
\usage{
plot_rating_comparison(cmp, old_label = "Old", new_label = "New")
}
\arguments{
\item{cmp}{The list returned by \code{\link[=compare_ratings]{compare_ratings()}}.}

\item{old_label, new_label}{Character. Legend labels for the two
ratings. Defaults \code{"Old"} and \code{"New"}.}
}
\value{
The combined grob from \code{gridExtra::grid.arrange()}, invisibly.
Printed as a side effect.
}
\description{
Two-panel figure for a \code{\link[=compare_ratings]{compare_ratings()}} result. The top panel
overlays the old and new rating curves (dashed vs solid, the same
convention \code{plot_rc_gaps()} uses for before/after), so the shape of
the change is visible directly. The bottom panel plots the discharge
difference against stage, so it's clear not just \emph{that} an amendment
changed the rating but \emph{where in the stage range} it actually matters
-- a coefficient change that only bites at high flows looks very
different from one that shifts the whole curve evenly.
}
\examples{
rating_old_dt <- data.table::data.table(
  lower_level = c(0.0, 1.2), upper_level = c(1.2, 3.0),
  C = c(2.5, 4.1), a = c(0, 0), n = c(1.5, 1.7)
)
rating_new_dt <- data.table::data.table(
  lower_level = c(0.0, 1.2), upper_level = c(1.2, 3.0),
  C = c(2.6, 4.0), a = c(0, 0), n = c(1.5, 1.7)
)
cmp <- compare_ratings(rating_old_dt, rating_new_dt)
plot_rating_comparison(cmp)

}
\seealso{
\code{\link[=compare_ratings]{compare_ratings()}}
}
