% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weir_equations.R
\name{weir_discharge_cipoletti}
\alias{weir_discharge_cipoletti}
\title{Discharge over a Cipoletti (trapezoidal) sharp-crested weir}
\usage{
weir_discharge_cipoletti(
  head_m,
  crest_length_m,
  u_cd,
  u_head_m,
  coverage_k = 2
)
}
\arguments{
\item{head_m}{Numeric vector. Head over the weir crest, m. Must be positive.}

\item{crest_length_m}{Single positive number. Crest length, m (the
longer, downstream edge of the trapezoidal notch).}

\item{u_cd}{Single non-negative number. Relative standard uncertainty
in the discharge coefficient (e.g. \code{0.02} for 2\%). See Description.}

\item{u_head_m}{Single non-negative number. Absolute standard
uncertainty in the measured head, m.}

\item{coverage_k}{Single positive number. GUM coverage factor for the
expanded uncertainty \code{U = coverage_k * u_c}. Default \code{2}
(approximately 95\% for a normally-distributed measurand, the
conventional GUM default -- JCGM 100:2008 §2.3.6).}
}
\value{
As \code{\link[=weir_discharge_rectangular]{weir_discharge_rectangular()}}.
}
\description{
The Cipoletti weir's trapezoidal notch (sides sloped 1 horizontal to 4
vertical) is designed so the side slopes' extra discharge compensates
for end-contraction losses, letting it use a single fixed coefficient
with no weir-height or approach-channel correction, unlike the
rectangular case:

\deqn{Q = 1.859 \, L \, H^{3/2}}

(SI form; \eqn{L} is crest length, \eqn{H} is head over the crest.
Derived here from the USBR \emph{Water Measurement Manual}'s published US
customary form, \eqn{Q_{cfs} = 3.367 \, L_{ft} H_{ft}^{3/2}}, by direct
unit conversion.)

That simplicity comes at a cost: the USBR manual describes overall
accuracy for a well-maintained Cipoletti installation as only around
\eqn{\pm 5\%} -- markedly worse than a rectangular or V-notch weir.
Uncertainty here still follows the same GUM quadrature as the other
structures (head exponent \code{3/2}); \code{u_cd} and \code{u_head_m} are required,
with that \eqn{\pm 5\%} figure as context for the scale of \code{u_cd} a
Cipoletti weir typically needs, not a substitute for it (it is an
aggregate accuracy figure, not already split into coefficient/head
components).
}
\examples{
weir_discharge_cipoletti(
  head_m = c(0.1, 0.2, 0.3), crest_length_m = 1.0,
  u_cd = 0.05, u_head_m = 0.003
)

}
\references{
USBR \emph{Water Measurement Manual}, ch. 7, §12. JCGM 100:2008 (GUM).
}
\seealso{
\code{\link[=weir_discharge_rectangular]{weir_discharge_rectangular()}}, \code{\link[=weir_discharge_vnotch]{weir_discharge_vnotch()}},
\code{\link[=flume_discharge_parshall]{flume_discharge_parshall()}}; \code{vignette("weir_flume_guide")}.
}
