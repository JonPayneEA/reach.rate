% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate_from_cross_section.R
\name{rate_from_cross_section}
\alias{rate_from_cross_section}
\title{Derive a theoretical rating from a surveyed cross-section (Manning's equation)}
\usage{
rate_from_cross_section(
  cross_section,
  slope,
  roughness,
  distance_col = "distance_m",
  elevation_col = "elevation_m",
  stage_seq = NULL,
  n_points = 100L,
  control = NULL,
  n_bounds = NULL,
  ...
)
}
\arguments{
\item{cross_section}{A data.frame/data.table of surveyed
distance/elevation points, at least 2 rows, already on \code{stage}'s own
vertical datum (see Description).}

\item{slope}{Single positive finite number. Bed/energy slope, m/m.}

\item{roughness}{Single positive finite number. Manning's \code{n}.}

\item{distance_col, elevation_col}{Character. Column names in
\code{cross_section}, matching \code{\link[=plot_rating_cross_section]{plot_rating_cross_section()}}'s own
defaults so the same object works with both functions. Default
\code{"distance_m"}/\code{"elevation_m"}.}

\item{stage_seq}{Numeric vector of stages to evaluate, or \code{NULL}
(default) for an evenly-spaced sequence of \code{n_points} stages from
just above the cross-section's lowest surveyed elevation to its
highest.}

\item{n_points}{Integer. Number of stages in the default \code{stage_seq}.
Ignored if \code{stage_seq} is supplied. Default \code{100L}.}

\item{control, n_bounds}{As in \code{\link[=rate_optimise]{rate_optimise()}}, passed straight
through to fit the synthetic series.}

\item{...}{Further arguments passed to \code{\link[=rate_optimise]{rate_optimise()}} (e.g.
\code{multi_start}, \code{objective}).}
}
\value{
A \link{FlodeRating} instance, identical in structure to
\code{\link[=rate_optimise]{rate_optimise()}}'s own return value.
}
\description{
Builds a stage-discharge rating from channel geometry alone, with no
gaugings: at each of a dense sequence of stages, \code{cross_section}'s
wetted area and wetted perimeter are found by trapezoidal integration
of the surveyed points, giving a hydraulic radius, and Manning's
equation (\code{Q = (1/n) * A * R^(2/3) * sqrt(S)}) converts that to a
discharge. That synthetic \verb{(stage, discharge)} series is then handed
straight to \code{\link[=rate_optimise]{rate_optimise()}}, so the returned \link{FlodeRating} carries
the same \code{C}/\code{a}/\code{n} limbs, break detection, and diagnostics as any
gauged fit -- the only difference is \code{fit@provenance$source}, which
reads \code{"cross_section_theoretical"} rather than \code{"gauged"}, and
\code{fit@gaugings}, which holds the synthetic points the Manning
calculation produced rather than real field measurements.

This is a standalone diagnostic: a first-cut rating at an ungauged or
newly-installed site, or a physically-grounded check on what a
surveyed section implies about the shape of the curve -- not (yet) a
way to extend an existing gauged rating past its top gauging. Since
\code{cross_section} is exactly the object \code{\link[=plot_rating_cross_section]{plot_rating_cross_section()}}
also takes, the two compose directly: overlay the returned fit against
the very geometry that generated it as a sanity check (see Examples).

A single Manning's \code{n} is applied across the whole wetted section.
Real compound channels (a defined low-flow channel plus overbank
berms with different roughness) are more accurately handled by the
divided-channel method, with a separate \code{n} per subsection -- not
supported here; treat a single-\code{n} result for a compound section as
approximate.

\code{cross_section} must reach at least as high as the highest stage
rated: this function does not assume the end points extend as
vertical banks above the survey's own top, so a \code{stage_seq} above
\code{max(elevation_col)} errors rather than silently understating the
true wetted perimeter. Supply a wider survey (including bank points)
to rate higher stages.
}
\examples{
# A simple trapezoidal survey (same shape plot_rating_cross_section()'s
# own example uses): a 6 m flat bed, 1.25:1 side slopes, 2.4 m banks.
xs <- data.frame(
  distance_m = c(-6, -3, 3, 6),
  elevation_m = c(2.4, 0, 0, 2.4)
)
fit <- rate_from_cross_section(xs, slope = 0.001, roughness = 0.035, n_points = 30)
fit@provenance$source

# Overlay the fit on the very geometry that generated it.
plot_rating_cross_section(fit, xs)

}
\seealso{
\code{\link[=rate_optimise]{rate_optimise()}}, which this function calls internally;
\code{\link[=plot_rating_cross_section]{plot_rating_cross_section()}} for overlaying the result on
\code{cross_section}; \code{\link[=graft_rating]{graft_rating()}} for joining a rating onto another
above its trusted range (a natural next step for a cross-section
rating used to extend a gauged one, not yet wired up here).
}
