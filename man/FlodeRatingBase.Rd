% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flode_classes.R
\name{FlodeRatingBase}
\alias{FlodeRatingBase}
\title{Abstract base class for a fitted rating (S7)}
\usage{
FlodeRatingBase(
  gaugings = (structure(function (.data) 

    stop2(sprintf("S3 class <\%s> doesn't have a constructor.", "data.table"), call =
    NULL), class = "S7_constructor"))(),
  fit_starts = NULL,
  status = "independently_fitted",
  provenance = list(),
  previous = NULL
)
}
\arguments{
\item{gaugings}{Data.table with \code{discharge_cms} and \code{stage_m}
columns -- the gaugings the fit was built from. May optionally
include a \code{gauging_datetime} column (\code{Date} or \code{POSIXct}) recording
when each spot gauging was taken. Nothing in the package currently
reads this column -- it's groundwork for a future age-aware fitting
option (see issue #9).}

\item{fit_starts}{Data.table of multi-start fitting attempts, or
\code{NULL} if the fit wasn't produced with \code{multi_start = TRUE}.}

\item{status}{Character. One of \code{"independently_fitted"},
\code{"post_fit_aligned"}, or \code{"constrained_refit"}.}

\item{provenance}{List recording how the fit was made (fitting
method, controls, bounds, tool version).}

\item{previous}{The exact pre-amendment fit this was built from (same
class as this instance), or \code{NULL} (default) for a fit with no such
history -- an independently-fitted result, for instance. Mirrors
\link{FlodeRatingTable}'s own \verb{@previous} audit chain.}
}
\description{
Properties and validation shared by \link{FlodeRating} and
\link{FlodeSegmentedRating}: the gaugings a fit was built from, multi-start
fitting bookkeeping (if any), a status flag distinguishing an
independently-fitted result from a post-fit amendment, a provenance
list for auditability (fitting method, controls, bounds, tool
version), and a \code{previous} chain for amendments produced from an
earlier fit of the same class. Never constructed directly -- see
\link{FlodeRating} and \link{FlodeSegmentedRating}.
}
