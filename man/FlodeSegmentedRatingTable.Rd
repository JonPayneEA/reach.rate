% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flode_classes.R
\name{FlodeSegmentedRatingTable}
\alias{FlodeSegmentedRatingTable}
\title{A segmented rating equation table (S7), as_rating_table()'s bridge for FlodeSegmentedRating}
\usage{
FlodeSegmentedRatingTable(
  table = (structure(function (.data) 

    stop2(sprintf("S3 class <\%s> doesn't have a constructor.", "data.table"), call =
    NULL), class = "S7_constructor"))(),
  C = numeric(0),
  gauged_upper_m = numeric(0),
  status = "independently_fitted",
  previous = NULL
)
}
\arguments{
\item{table}{Data.table with \code{segment} (\verb{1..k}), \code{bp}, and \code{n}
columns, one row per segment, \code{bp} strictly increasing by segment.}

\item{C}{Single positive number. The shared scale coefficient every
segment's factor multiplies into (see \code{\link[=rate_optimise_segmented]{rate_optimise_segmented()}}'s
\code{Q = C * max(H - bp_1, 0)^n_1 * prod_(j>=2) (max(H - bp_j, 0) + 1)^n_j}).}

\item{gauged_upper_m}{Single number. The highest stage in the gaugings
the originating fit was built from -- used to flag extrapolation.}

\item{status}{Character. One of \code{"independently_fitted"},
\code{"post_fit_aligned"}.}

\item{previous}{The exact pre-amendment \link{FlodeSegmentedRatingTable}
this one was built from, or \code{NULL} if it has no such prior identity.}
}
\description{
The structural analogue of \link{FlodeRatingTable} for
\link{FlodeSegmentedRating}: one row per segment (\code{bp}, \code{n}), plus the
single shared \code{C} every segment's factor multiplies into. Unlike
\code{FlodeRatingTable}'s rows, a row here is not independently evaluable
-- discharge at a stage above the lowest breakpoint is the product of
every segment factor active at that stage (see
\code{vignette("segmented_model_guide")}), so this deliberately does not
inherit from \code{FlodeRatingTable} and is not accepted by that class's
methods (\code{\link[=detect_rc_gaps]{detect_rc_gaps()}} included -- a segmented rating has no
junction to check by construction).

\verb{@gauged_upper_m} exists so \code{\link[=apply_rating]{apply_rating()}}/\code{\link[=apply_rating_inverse]{apply_rating_inverse()}}
can still flag extrapolation correctly once bridged to this table:
unlike a breakpoint, the highest gauged stage isn't recoverable from
\code{C}/\code{bp}/\code{n} alone, so \code{\link[=as_rating_table]{as_rating_table()}} carries it over from the
originating fit's own gaugings at construction time.
}
