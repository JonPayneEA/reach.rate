% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rating_curve_demo.R
\name{run_demo}
\alias{run_demo}
\title{Run the fit -> flag -> expand -> detect -> resolve -> plot pipeline}
\usage{
run_demo(plot = TRUE)
}
\arguments{
\item{plot}{Logical. Draw \code{rating_plot()} and \code{plot_rc_gaps()} as a
side effect. Default \code{TRUE}.}
}
\value{
A list with elements \code{fit} (the \link{FlodeRating} instance, with
\code{doubtful} flagged in \verb{@limbs}), \code{gaps} (the junction gap report),
\code{rc_raw} (the expanded table before resolution), and \code{rc_fixed}
(after resolution).
}
\description{
A worked example showing \code{rate_optimise} and \code{gap_check} operating as a
single pipeline: gaugings go in, a fitted multi-limb rating comes out,
limbs that extrapolate beyond their own gaugings are flagged doubtful,
the fit is bridged to a rating table and expanded to stage-discharge
rows, junction gaps between independently-fitted limbs are detected
and resolved, and both stages are plotted.
}
\examples{
result <- run_demo(plot = FALSE)
result$gaps

}
