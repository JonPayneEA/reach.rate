% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_rating.R
\name{apply_rating_versioned}
\alias{apply_rating_versioned}
\title{Apply a versioned rating to a stage time series}
\usage{
apply_rating_versioned(
  stage_dt,
  rating_history_dt,
  stage_col = "stage",
  datetime_col = "datetime",
  out_col = "discharge"
)
}
\arguments{
\item{stage_dt}{Data.frame or data.table with a stage column and a
datetime column.}

\item{rating_history_dt}{Data.table with one row per (version, limb):
\code{version}, \code{effective_from}, \code{effective_to} (both POSIXct or Date;
\code{effective_to = NA} means "still current", and only the most recent
version may have \code{NA} here), \code{lower_level}, \code{upper_level}, \code{C}, \code{a},
\code{n}. Version date ranges must not overlap.}

\item{stage_col, datetime_col}{Character. Column names in \code{stage_dt}.
Defaults \code{"stage"}, \code{"datetime"}.}

\item{out_col}{Character. Default \code{"discharge"}.}
}
\value{
\code{stage_dt} as a data.table with \code{out_col}, \code{extrapolated}, and
\code{version} (which rating version was applied to that row; \code{NA} if no
version was in effect at that timestamp, in which case \code{out_col} is
also \code{NA} for that row and a warning is issued) columns added.
}
\description{
Real ratings shift over time -- bed erosion, deposition, vegetation
growth, a channel realignment -- which is exactly why gauging
stations get re-rated periodically (Hodson et al. 2024, citing
Mansanarez et al. 2019, "Shift Happens!"). \code{\link[=apply_rating]{apply_rating()}} assumes a
single, static rating for an entire stage series; this instead
selects, for each stage observation, whichever rating version was in
effect at that observation's timestamp, then applies that version's
equation -- composing \code{\link[=apply_rating]{apply_rating()}} once per version rather than
reimplementing the discharge calculation.
}
\examples{
rating_history_dt <- data.table::data.table(
  version = c("v1", "v2"),
  effective_from = as.POSIXct(c("2024-01-01", "2025-06-01"), tz = "UTC"),
  effective_to = as.POSIXct(c("2025-06-01", NA), tz = "UTC"),
  lower_level = c(0.0, 0.0), upper_level = c(3.0, 3.0),
  C = c(3.0, 3.4), a = c(0, 0), n = c(1.6, 1.6)
)
stage_dt <- data.table::data.table(
  datetime = as.POSIXct(c("2024-06-01", "2025-12-01"), tz = "UTC"),
  stage = c(1.5, 1.5)
)
apply_rating_versioned(stage_dt, rating_history_dt)

}
\seealso{
\code{\link[=apply_rating]{apply_rating()}}
}
