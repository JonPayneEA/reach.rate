% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate_optimise.R
\name{rate_optimise}
\alias{rate_optimise}
\title{Fit a multi-limb power-law rating curve}
\usage{
rate_optimise(
  discharge_cms,
  stage_m,
  gauging_datetime = NULL,
  control = NULL,
  n_boot = 0L,
  boot_seed = NULL,
  min_boot_success = 0.8,
  multi_start = TRUE,
  n_bounds = NULL,
  objective = c("absolute", "relative"),
  age_halflife = NULL,
  age_min_weight = 0.1,
  reference_datetime = NULL,
  ...
)
}
\arguments{
\item{discharge_cms}{Numeric vector. Gauged discharge, m\eqn{^3}/s.}

\item{stage_m}{Numeric vector. Gauged stage, m. Same length as
\code{discharge_cms}.}

\item{gauging_datetime}{\code{Date}/\code{POSIXct} vector or \code{NULL}. When each
gauging in \code{discharge_cms}/\code{stage_m} was taken, same length as
\code{discharge_cms} if supplied. Stored on the returned rating's
\verb{@gaugings}; only affects the fit itself if \code{age_halflife} is also
supplied (see below). Default \code{NULL}: no dates recorded, matching
today's behaviour.}

\item{control}{Numeric vector or \code{NULL}. Stage breakpoints separating
rating limbs, e.g. \code{c(1.6, 2.2)} for three limbs. \code{NULL} (default)
fits a single limb across the full range.}

\item{n_boot}{Integer. Number of bootstrap refits per limb, for
coefficient uncertainty. Default \code{0L}: no bootstrap.}

\item{boot_seed}{Integer or \code{NULL}. Passed to \code{set.seed()} before
bootstrapping, with the caller's prior RNG state saved and restored
afterward. Default \code{NULL}.}

\item{min_boot_success}{Numeric in \verb{[0, 1]}. Warn if the successful-draw
fraction for a limb falls below this. Default \code{0.8}.}

\item{multi_start}{Logical. If \code{TRUE} (default), each limb is fitted
from several starting-value combinations and the best converged,
domain-valid result is kept. Set \code{FALSE} for the original
single-start behaviour.}

\item{n_bounds}{Numeric vector \code{c(lower, upper)}, or \code{NULL} (default).
Box-constrains the exponent \code{n} for every limb, e.g. to a
theoretical range implied by known channel-control hydraulics
(roughly 1.5 for a rectangular control, 2.5 for triangular/V-notch).
The top of a rating is usually the least-gauged part of the record,
so this lets known geometry keep the fit realistic there without
more data. Set \code{lower == upper} to fix \code{n} outright. \code{NULL}
(default) keeps today's effectively unconstrained \code{n > 0}. Not a
default constraint -- most callers don't know their channel-control
type, and an unconstrained fit remains the default behaviour.}

\item{objective}{Character. \code{"absolute"} (default) minimises absolute
discharge error, as before. \code{"relative"} fits \code{log(discharge_cms)}
residuals instead -- gauging error is typically proportional (a
percentage of the reading) rather than a fixed absolute amount, so
this can better balance low- and high-flow structure than absolute
error does. Requires \code{discharge_cms} to be strictly positive.
Reported diagnostics (\code{rmse_cms}, \code{r_squared}, and the error columns)
are always recomputed on the natural discharge scale regardless of
\code{objective}, so they stay comparable between the two.  Not a
default -- existing callers see no change in behaviour.}

\item{age_halflife}{Single positive number of days, or \code{NULL}
(default). Opt-in recency weighting: each gauging's residual is
weighted by \code{age_min_weight + (1 - age_min_weight) * 0.5^(age_days / age_halflife)}, where \code{age_days} is its age (clamped
at 0) relative to \code{reference_datetime}. \code{NULL} (default) fits
unweighted, exactly as before -- supplying \code{gauging_datetime} alone,
with \code{age_halflife} left \code{NULL}, changes nothing about the fit.
Requires \code{gauging_datetime}. See \code{vignette("recency_weighting_guide")}
for the full derivation and a worked example.}

\item{age_min_weight}{Single number in \verb{[0, 1)}. The weight floor a
gauging's age can never fall below, however old -- prevents an
old-but-genuinely-informative gauging (e.g. a limb's one high-flow
flood) from being weighted to near-zero purely for being old. Not
magnitude-aware: an old extreme gauging and an old ordinary one
decay identically. Default \code{0.1}. Ignored if \code{age_halflife} is
\code{NULL}.}

\item{reference_datetime}{\code{Date}/\code{POSIXct} or \code{NULL} (default). The
"as of" point ages are measured back from. \code{NULL} uses
\code{max(gauging_datetime)} -- age relative to this dataset's own most
recent gauging. Ignored if \code{age_halflife} is \code{NULL}.}

\item{...}{Passed to \code{minpack.lm::nlsLM()} itself for the primary fit
only, not the bootstrap refits.}
}
\value{
A \link{FlodeRating} instance. \verb{@limbs} has one row per limb with
bounds, coefficients \code{C}/\code{a}/\code{n}, fit diagnostics -- including
\code{rmse_pct} (a relative/percentage RMSE alongside the absolute
\code{rmse_cms}, always computed regardless of \code{objective}) and
\code{C_se_asymp}/\code{a_se_asymp}/\code{n_se_asymp} (closed-form asymptotic
parameter standard errors from the fit's own NLS covariance matrix,
always computed, \code{NA} if that covariance matrix is singular --
distinct from the bootstrap-only \code{C_se}/\code{a_se}/\code{n_se} columns below)
-- and multi-start bookkeeping. When \code{n_boot > 0}, \verb{@bootstrap} holds
every draw and \verb{@limbs} gains \code{C_se}/\code{a_se}/\code{n_se}/percentile/
success-count columns; prefer these over \verb{*_se_asymp} when present,
since they don't rely on the NLS asymptotic approximation.
\verb{@gaugings} holds the input data with a \code{limb} column (and
\code{gauging_datetime} if supplied, and \code{age_weight} if \code{age_halflife}
was supplied). \verb{@fit_starts} holds every multi-start attempt when
\code{multi_start = TRUE}. \verb{@status} is \code{"independently_fitted"}.
}
\description{
Fits the rating equation \eqn{Q = C(H - a)^n} by Levenberg-Marquardt
non-linear least squares (\code{minpack.lm::nlsLM}). A single limb is fitted
across the full stage range when \code{control} is \code{NULL}; otherwise the
gaugings are split into limbs at each stage in \code{control} and each limb
is fitted independently -- this fits each limb separately, not a
single globally-optimised multi-limb model (see
\code{rate_optimise_segmented()} for that).
}
\examples{
discharge_cms <- c(177.685, 240.898, 221.954, 205.55, 383.051, 154.061, 216.582)
stage_m <- c(1.855, 2.109, 2.037, 1.972, 2.574, 1.748, 2.016)
fit <- rate_optimise(discharge_cms, stage_m)
fit@limbs[, .(limb, C, a, n, rmse_cms, r_squared)]

}
\seealso{
\code{\link[=rating_plot]{rating_plot()}}, \code{\link[=plot_rating_residuals]{plot_rating_residuals()}},
\code{\link[=flag_extrapolated_limbs]{flag_extrapolated_limbs()}}, \code{\link[=suggest_breakpoints]{suggest_breakpoints()}},
\code{\link[=rate_optimise_constrained]{rate_optimise_constrained()}}
}
